
interface BlosumTabProps {
    blocks: string[][][];
    pValues: {
        letter: string;
        qValue: number;
    }[];
}

export function BlosumTab({ blocks, pValues }: BlosumTabProps) {

    return (
        <div>
        </div>
    )
}